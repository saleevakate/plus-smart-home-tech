spring:
  application:
    name: api-gateway
  cloud:
    config:
      enabled: false
  config:
    import: ""
  main:
    allow-bean-definition-overriding: true

server:
  port: 0

app:
  security:
    users:
      - username: ivan
        password: ivan
        roles: USER
      - username: anna
        password: anna
        roles: USER, ADMIN